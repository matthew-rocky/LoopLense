services:
  - type: web
    name: looplense
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && bash scripts/build_render.sh
    startCommand: python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT
